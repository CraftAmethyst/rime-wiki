{
  "name": "vitepress-project",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vitepress dev docs",
    "build": "vitepress build docs",
    "serve": "vitepress serve docs",
    "deploy": "pnpm run build && pnpm dlx wrangler pages deploy docs/.vitepress/dist --project-name rime-wiki"
  },
  "devDependencies": {
    "vue": "3.2.44",
    "vitepress": "1.0.0-alpha.28"
  },
  "pnpm": {
    "peerDependencyRules": {
      "ignoreMissing": [
        "@algolia/client-search"
      ]
    }
  }
}
